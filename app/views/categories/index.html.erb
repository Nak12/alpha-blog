<h1>Listening all categories</h1>

<% @categories.each do |category| %>
  <ul>
    <div class="row">
      <div class="card col-md-4 text-center">
        <li><%= link_to "#{category.name}", category_path(category) %></li>
      </div>
    </div>
  </ul>
<% end %> 
<%= will_paginate %>